to run locally:
npx tinacms dev -c "bundle exec jekyll serve"

to create svg clickable map:
use inkscape to import svg, then draw transparent rectangles over the areas you want to be clickable.
Then save as optimized svg. Then add .svg.html in includes folder. Make sure it's well formatted, use others as example
